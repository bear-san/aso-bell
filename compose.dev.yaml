services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes: ["mongo-dev-data:/data/db"]
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.runCommand({ ping: 1 }).ok"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongo-dev-data:
